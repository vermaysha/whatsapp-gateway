{
  "name": "hash",
  "version": "1.0.0",
  "main": "dist/hash.js",
  "module": "dist/hash.mjs",
  "types": "dist/hash.d.ts",
  "license": "UNLICENSED",
  "private": true,
  "files": [
    "dist/**"
  ],
  "scripts": {
    "build": "rimraf dist && tsup",
    "dev": "tsup --watch"
  },
  "dependencies": {
    "argon2": "^0.31.1",
    "dotenv": "^16.3.1",
    "tsconfig": "workspace:^"
  },
  "devDependencies": {
    "@types/node": "^20.8.6",
    "rimraf": "^5.0.5",
    "tsup": "^7.2.0",
    "typescript": "^5.2.2"
  }
}
