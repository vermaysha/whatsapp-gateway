{
  "name": "pagination",
  "version": "1.0.0",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "license": "UNLICENSED",
  "private": true,
  "files": [
    "dist/**"
  ],
  "scripts": {
    "build": "rimraf dist && tsup",
    "dev": "tsup --watch",
    "format": "prettier --write \"src/**/*.ts\"",
    "lint": "eslint \"**/*.ts\" --fix"
  },
  "dependencies": {
    "argon2": "^0.31.1",
    "dotenv": "^16.3.1",
    "tsconfig": "workspace:^"
  },
  "devDependencies": {
    "@types/node": "^20.8.0",
    "rimraf": "^5.0.5",
    "tsup": "^7.2.0",
    "typescript": "^5.2.2"
  }
}
